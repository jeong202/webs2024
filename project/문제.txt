totalScore가 100점일때 class="en"에 a+
totalScore가 70~90점일때 class="en"에 b+
totalScore가 40~60일때 class="en"에 c+
totalScore가 0-30점일 때 class="en"에 f






 // totalScore = 종합점수

        // totalScore에 따라 grade를 결정
        let grade;

        if (totalScore >= 90) {
            grade = 'a+';

        } else if (totalScore >= 70) {
            grade = 'b+';
        } else if (totalScore >= 40) {
            grade = 'c+';
        } else {
            grade = 'f';
        }








totalScore가 100점일때 class="en"에 a+

툭 치기만 해도 '요즘 유행'이 자동으로 나오고,
주변인들과 함께 공유하고 즐기는 당신은 진정한 힙쟁이!
빠른 트렌드 섭렵과, 그때그때마다 업데이트 되는
당신의 SNS를 참고하는 주변인들도 있을 듯 하네요.

totalScore가 70~90점일때 class="en"에 b+
굵직굵직한 유행엔 빠삭한 당신,
최신 유행을 꽉 쥐고 있는 듯 하지만, 한발짝 느린 편이에요.
그래도 SNS에서 적극적으로 최신 트렌드를 섭렵하려 하고,
곧잘 자연스럽게 스며들어요

totalScore가 40~60일때 class="en"에 c+
과거 한때엔 나름
유행에 관심이 많았던 당신,
지금은 현생에 치여
점차 트렌드로부터
멀어지고 있는 듯 하네요
"그걸 꼭 알아야 돼?"
싶긴 하지만
가끔은 요즘 트렌드가
궁금해질 때도 있어요.

totalScore가 0-30점일 때 class="en"에 f
아직도 2000년대에 살고 있는 당신,
요즘 유행엔 눈 하나 깜빡 안 해요
아직도 옛날 노래,옛날 예능,옛날 드라마가 더 재밌어요
가끔은 요즘 유행을 이해해보려고 노력은 하지만 너무 어려워요


totalScore의 값은 <div class="num">


totalScore의 값이 0~100일때 사이의 값을 가진다면

totalScore가 100점일때는 gradeEnd배열의 첫번채 객체의 gradeEn의 값이
<div class="en"></div>에, 그리고 graDesc의 값이 <div class="desc"></div>

totalScore의 값이 70~90점일때는 gradeEnd배열의 두번채 객체의 gradeEn의 값이
<div class="en"></div>에, 그리고 graDesc의 값이 <div class="desc"></div>

totalScore의 값이 40~60점일때는 gradeEnd배열의 세번채 객체의 gradeEn의 값이
<div class="en"></div>에, 그리고 graDesc의 값이 <div class="desc"></div>

totalScore의 값이 0~30점일때는 gradeEnd배열의 네번채 객체의 gradeEn의 값이
<div class="en"></div>에, 그리고 graDesc의 값이 <div class="desc"></div>
들어가도록